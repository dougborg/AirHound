name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, ready_for_review]

jobs:
  commitlint:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          fetch-depth: 0
      - uses: wagoid/commitlint-github-action@b948419dd99f3fd78a6548d48f94e3df7f6bf3ed # v6

  fmt:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
      - uses: dtolnay/rust-toolchain@0f1b44df7e9cbb178d781a242338dfa5e243ad7f # nightly
        with:
          components: rustfmt
      - run: cargo fmt --check
        env:
          RUSTUP_TOOLCHAIN: nightly

  schemas:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
      - uses: extractions/setup-just@f8a3cce218d9f83db3a2ecd90e41ac3de6cdfd9b # v3
      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6
        with:
          python-version: "3.x"
      - run: pip install check-jsonschema
      - name: Check JSON formatting
        run: just check-json
      - name: Validate schemas (metaschema)
        run: just check-schemas
      - name: Validate examples against schemas
        run: just check-examples

  test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
      - uses: dtolnay/rust-toolchain@0f1b44df7e9cbb178d781a242338dfa5e243ad7f # nightly
      - run: cargo test --lib --no-default-features
        env:
          RUSTUP_TOOLCHAIN: nightly

  audit:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
      - uses: dtolnay/rust-toolchain@0f1b44df7e9cbb178d781a242338dfa5e243ad7f # nightly
      - name: Install cargo-audit
        run: cargo install cargo-audit --locked
        env:
          RUSTUP_TOOLCHAIN: nightly
      - name: Audit dependencies
        run: cargo audit
        env:
          RUSTUP_TOOLCHAIN: nightly

  build:
    runs-on: ubuntu-latest
    container:
      image: espressif/idf-rust:all_1.90.0.0
      options: --user root
      env:
        RUSTUP_HOME: /home/esp/.rustup
        CARGO_HOME: /home/esp/.cargo
    strategy:
      matrix:
        include:
          - name: xiao
            target: xtensa-esp32s3-none-elf
            features: xiao
          - name: m5stickc
            target: xtensa-esp32-none-elf
            features: m5stickc
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Build ${{ matrix.name }}
        run: |
          export PATH="/home/esp/.cargo/bin:$PATH"
          . /home/esp/export-esp.sh
          cargo build --no-default-features --features ${{ matrix.features }} --release --target ${{ matrix.target }} -Z build-std=core,alloc

      - uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7
        with:
          name: elf-${{ matrix.name }}
          path: target/${{ matrix.target }}/release/airhound
          retention-days: 7

  build-std:
    runs-on: ubuntu-latest
    container:
      image: espressif/idf-rust:all_1.90.0.0
      options: --user root
      env:
        RUSTUP_HOME: /home/esp/.rustup
        CARGO_HOME: /home/esp/.cargo
    strategy:
      matrix:
        include:
          - name: xiao-std
            target: xtensa-esp32s3-espidf
            chip: esp32s3
            features: xiao
          - name: m5stickc-std
            target: xtensa-esp32-espidf
            chip: esp32
            features: m5stickc
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Build ${{ matrix.name }}
        run: |
          export PATH="/home/esp/.cargo/bin:$PATH"
          . /home/esp/export-esp.sh
          cd firmware-std && MCU=${{ matrix.chip }} cargo build --no-default-features --features ${{ matrix.features }} --release --target ${{ matrix.target }} -Z build-std=std,panic_abort

      - uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7
        with:
          name: elf-${{ matrix.name }}
          path: firmware-std/target/${{ matrix.target }}/release/airhound-std
          retention-days: 7

  # Wokwi simulation smoke test — add WOKWI_CLI_TOKEN repo secret to enable
  wokwi:
    needs: build
    runs-on: ubuntu-latest
    env:
      WOKWI_CLI_TOKEN: ${{ secrets.WOKWI_CLI_TOKEN }}
    steps:
      - if: env.WOKWI_CLI_TOKEN != ''
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - if: env.WOKWI_CLI_TOKEN != ''
        uses: actions/download-artifact@70fc10c6e5e1ce46ad2ea6f2b72d43f7d47b13c3 # v8
        with:
          name: elf-m5stickc
          path: target/xtensa-esp32-none-elf/release/

      - name: Simulate firmware
        if: env.WOKWI_CLI_TOKEN != ''
        uses: wokwi/wokwi-ci-action@11ddc3fe66a14fcd46683fb21ae5c77657ad4606 # v1
        with:
          token: ${{ secrets.WOKWI_CLI_TOKEN }}
          timeout: 10000
          expect_text: "AirHound v"

  preview-binaries:
    if: github.event_name == 'pull_request' && !github.event.pull_request.draft
    needs: build
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - name: xiao
            chip: esp32s3
          - name: m5stickc
            chip: esp32
    steps:
      - uses: cargo-bins/cargo-binstall@bc432b49369a3f25c8c8b19578a82060c18a5dd6 # main

      - name: Install espflash
        run: cargo binstall espflash --no-confirm

      - uses: actions/download-artifact@70fc10c6e5e1ce46ad2ea6f2b72d43f7d47b13c3 # v8
        with:
          name: elf-${{ matrix.name }}

      - name: Create flashable binary
        run: espflash save-image --chip ${{ matrix.chip }} airhound airhound-${{ matrix.name }}.bin

      - uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7
        with:
          name: bin-${{ matrix.name }}
          path: airhound-${{ matrix.name }}.bin
          retention-days: 14

  preview-binaries-std:
    if: github.event_name == 'pull_request' && !github.event.pull_request.draft
    needs: build-std
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - name: xiao-std
            chip: esp32s3
          - name: m5stickc-std
            chip: esp32
    steps:
      - uses: cargo-bins/cargo-binstall@bc432b49369a3f25c8c8b19578a82060c18a5dd6 # main

      - name: Install espflash
        run: cargo binstall espflash --no-confirm

      - uses: actions/download-artifact@70fc10c6e5e1ce46ad2ea6f2b72d43f7d47b13c3 # v8
        with:
          name: elf-${{ matrix.name }}

      - name: Create flashable binary
        run: espflash save-image --chip ${{ matrix.chip }} airhound-std airhound-${{ matrix.name }}.bin

      - uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7
        with:
          name: bin-${{ matrix.name }}
          path: airhound-${{ matrix.name }}.bin
          retention-days: 14

  preview-comment:
    name: Post Artifact Links
    if: github.event_name == 'pull_request' && !github.event.pull_request.draft && !cancelled()
    needs: [preview-binaries, preview-binaries-std]
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      actions: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          sparse-checkout: .github/scripts
      - uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd # v7
        continue-on-error: true
        with:
          script: |
            const script = require('./.github/scripts/preview-comment.js');
            await script({ github, context, needs: ${{ toJSON(needs) }} });
